// M0: engine skeleton only. No UI yet — see CLAUDE.md build order.
// `npm run sim` runs the headless wrestler-generation harness this milestone produces.
export default function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-neutral-950 p-6 text-center text-neutral-300">
      <p>
        Wrestling Booking Game — M0 (engine skeleton). Nothing renders yet.
        <br />
        Run <code className="text-neutral-100">npm run sim</code> to generate the roster.
      </p>
    </div>
  );
}
